name: CI/CD

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      # `check` embarque desormais testIntegration : on compile/package seulement ici,
      # les deux campagnes de tests ont leur propre etape ci-dessous.
      - name: Build
        run: ./gradlew assemble

      - name: Run tests
        run: ./gradlew test

      - name: Publish Test
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          check_name: Unit tests results
          files: |
            build/test-results/test/*.xml

      - name: Run integration tests
        run: ./gradlew testIntegration

      - name: Publish Integration Test
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          check_name: Integration tests results
          files: |
            build/test-results/testIntegration/*.xml

      - name: Generate aggregated JaCoCo report
        run: ./gradlew jacocoFullReport

      - name: JaCoCo Code Coverage Report
        id: jacoco_reporter
        uses: PavanMudigonda/jacoco-reporter@v4.8
        with:
          coverage_results_path: build/reports/jacoco/jacocoFullReport/jacocoFullReport.xml
          coverage_report_name: Coverage
          coverage_report_title: JaCoCo
          github_token: ${{ secrets.GITHUB_TOKEN }}
          skip_check_run: false
          minimum_coverage: 80
          fail_below_threshold: false
          publish_only_summary: false

      - name: Publish Coverage Summary
        run: cat _TMP/coverage-results.md >> $GITHUB_STEP_SUMMARY

      - name: Run mutation tests
        run: ./gradlew pitest
