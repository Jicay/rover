# Profil dedie aux tests d'integration : l'URL, l'utilisateur et le mot de passe sont
# injectes a chaud par Testcontainers (@DynamicPropertySource), le reste est ici.
spring:
  liquibase:
    change-log: classpath:db/changelog.xml
logging:
  level:
    liquibase: WARN
